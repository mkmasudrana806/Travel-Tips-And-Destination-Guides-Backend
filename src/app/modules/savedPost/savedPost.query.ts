export const POST_SAVED_QUERY_OPTIONS = {
  searchableFields: ["postTitle", "postCategory"],
  filterableFields: ["postCategory"],
  sortableFields: ["createdAt", "updatedAt"],
  selectableFields: ["postTitle", "postCategory", "createdAt", "updatedAt"],
};
